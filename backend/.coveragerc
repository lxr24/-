[run]
concurrency = thread,greenlet
source = app
parallel = true

[report]
show_missing = true
exclude_lines =
    pragma: no cover
    if __name__ == .__main__.:
    await db.connect\(\)
    await db.disconnect\(\)
    await init_db\(\)
    raise NotImplementedError